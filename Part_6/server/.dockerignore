# Временные swap-файлы Vim
*.swp

# Архив с dockle
dockle_0.4.15_Linux-64bit.tar.gz

# Лицензия и документация
LICENSE
README.md

# Dockerfile — исключаем, чтобы не попадал в образ
Dockerfile

# Локальные конфиги, если они не используются напрямую в COPY
nginx.conf

# Убедимся, что сам .dockerignore тоже не попадает
.dockerignore

# Файл сборки сервера (если бинарник генерируется и потом не нужен)
my_server

# Всё, что касается утилит/вспомогательных скриптов, если они не нужны в образе
.entrypoint.sh.swp
.Makefile.swp

